name: Output to comment
on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  generating-text:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - run: ls -la
      - run: .github/workflows/scripts/send-comment.sh
        shell: bash